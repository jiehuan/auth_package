# 核心框架
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Azure AD认证（替代Spring Security OIDC）
fastapi-azure-auth>=4.0.0

# Graph API HTTP调用（替代Java Microsoft Graph SDK）
httpx>=0.27.0

# Azure身份认证（SPN / ClientSecretCredential）
azure-identity>=1.16.0

# 配置管理（替代Spring @ConfigurationProperties）
pydantic-settings>=2.0.0

# 缓存（getGroupIdNameMapping TTL缓存）
cachetools>=5.3.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
