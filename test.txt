- step:
    type: Run
    name: inspect_plugin
    identifier: inspect_plugin
    spec:
      connectorRef: jfrog0122axio
      image: artifactory.platform.axaxl-cloud.com/toolingforge/plugins/xlc-panda-buildah:release-3.1.12
      shell: Sh
      command: |-
        echo "=== 这个 json 是什么 ==="
        cat plugin-build-tool.json

        echo "=== 镜像里有哪些可执行文件 ==="
        ls -la /usr/local/bin /usr/bin /opt 2>/dev/null | head -60

        echo "=== 找 panda / buildah 相关的东西 ==="
        find / -maxdepth 4 \( -name "*panda*" -o -name "*buildah*" -o -name "*plugin*" \) \
             -not -path "*/proc/*" 2>/dev/null | head -30

        echo "=== 环境变量(plugin 靠 PLUGIN_* 传参) ==="
        env | sort
