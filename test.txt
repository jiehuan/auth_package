echo "===== WORKSPACE ====="
pwd
ls -la

echo "===== ORIGINAL DOCKERFILE ====="
ls -l Dockerfile
sha256sum Dockerfile
head -20 Dockerfile

echo "===== CREATE PROBE ====="
cat > Dockerfile.probe <<'EOF'
FROM artifactory.platform.axaxl-cloud.com/docker/nginxinc/nginx-unprivileged:stable-alpine
RUN echo "PANDA_PROBE_REACHED_DOCKERFILE"
CMD ["nginx", "-g", "daemon off;"]
EOF

echo "probe" > probe.txt

ls -l Dockerfile.probe probe.txt
cat Dockerfile.probe



