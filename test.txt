set -eux

echo "===== ENV ====="
pwd
whoami
id

echo "===== WORKSPACE ====="
ls -ld /harness
ls -la /harness
ls -l /harness/Dockerfile.probe
cat /harness/Dockerfile.probe

echo "===== BUILDAH ====="
buildah --version
buildah info

echo "===== MINIMAL BUILDAH TEST ====="
buildah from scratch
buildah containers

echo "===== ACTUAL BUILD TEST ====="
buildah bud --log-level=debug \
  -f /harness/Dockerfile.probe \
  -t panda-probe \
  /harness
